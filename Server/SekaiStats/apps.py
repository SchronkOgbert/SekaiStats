"""
this is more of a config file rather than an app
"""


from django.apps import AppConfig


class SekaistatsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'SekaiStats'
